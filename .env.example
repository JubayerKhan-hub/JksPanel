# Copy this file to .env and fill in values
DISCORD_TOKEN=
# Comma-separated Discord user IDs that are bot owners (can use owner-only commands)
OWNER_IDS=
# Optional: Comma-separated guild IDs for faster slash command sync during development
GUILD_IDS=
# Optional: OpenAI key to enable /chat command
OPENAI_API_KEY=
# Optional: Set log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO